# 价值链网络可视化设计文档

**创建时间**: 2025-01-23  
**版本**: 2.0  
**状态**: ✅ **多流程网络结构可视化设计方案**

---

## 🎯 设计理念

**核心理念**：价值链是一个**复杂的网络结构**，不是单一的线性流程，而是由：
- **多个投资输入**（投资到6个不同领域）
- **多个核心资产+能力**（6个领域，每个领域有资产和能力）
- **多个流程**（6个关键流程）
- **多个价值要素**（产品特性、产品内在价值、客户感知价值、客户体验价值）
- **多个收益输出**（首单、追销、复购）

构成的**网络链**。

---

## 📊 网络结构可视化设计

### 整体布局（网络图结构）

```
┌─────────────────────────────────────────────────────────────────────────┐
│              价值链网络分析仪表盘（一页式）                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              价值链网络图（主区域，70%高度）                      │   │
│  │                                                                 │   │
│  │  ┌──────┐                                                       │   │
│  │  │ 投资 │                                                       │   │
│  │  └──┬───┘                                                       │   │
│  │     │                                                           │   │
│  │     ├─────────────────────────────────────────────────────┐   │   │
│  │     │                                                       │   │   │
│  │  ┌──▼────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐   │   │   │
│  │  │生产   │  │播传 │  │首单 │  │交付 │  │追销 │  │复购 │   │   │   │
│  │  │资产+  │  │资产+│  │资产+│  │资产+│  │资产+│  │资产+│   │   │   │
│  │  │能力   │  │能力 │  │能力 │  │能力 │  │能力 │  │能力 │   │   │   │
│  │  └───┬───┘  └──┬──┘  └──┬──┘  └──┬──┘  └──┬──┘  └──┬──┘   │   │   │
│  │      │         │        │        │        │        │      │   │   │
│  │  ┌───▼───┐  ┌─▼───┐  ┌─▼───┐  ┌─▼───┐  ┌─▼───┐  ┌─▼───┐  │   │   │
│  │  │生产   │  │播传 │  │首单 │  │交付 │  │追销 │  │复购 │  │   │   │
│  │  │流程   │  │流程 │  │流程 │  │流程 │  │流程 │  │流程 │  │   │   │
│  │  └───┬───┘  └──┬──┘  └──┬──┘  └──┬──┘  └──┬──┘  └──┬──┘  │   │   │
│  │      │         │        │        │        │        │      │   │   │
│  │      └─────────┼────────┼────────┼────────┼────────┘      │   │   │
│  │                │        │        │        │              │   │   │
│  │        ┌───────▼────────▼────────▼────────▼───────┐       │   │   │
│  │        │  价值要素层（网络交汇点）                  │       │   │   │
│  │        │  ┌────────┬────────┬────────┬────────┐  │       │   │   │
│  │        │  │产品特性│产品内在│客户感知│客户体验│  │       │   │   │
│  │        │  │  价值  │  价值  │  价值  │  价值  │  │       │   │   │
│  │        │  └────────┴────────┴────────┴────────┘  │       │   │   │
│  │        └─────────────────┬──────────────────────┘       │   │   │
│  │                          │                               │   │   │
│  │        ┌─────────────────▼──────────────────────┐       │   │   │
│  │        │  收益层（输出）                          │       │   │   │
│  │        │  ┌────┬────┬────┐                       │       │   │   │
│  │        │  │首单│追销│复购│                       │       │   │   │
│  │        │  │收入│收入│收入│                       │       │   │   │
│  │        │  └────┴────┴────┘                       │       │   │   │
│  │        └─────────────────────────────────────────┘       │   │   │
│  │                                                           │   │   │
│  └───────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │              关键指标与优化点（底部区域，30%高度）              │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │  ┌─────────────┬─────────────┬─────────────┬─────────────┐     │   │
│  │  │投入层指标   │资产层指标   │价值层指标   │收益层指标   │     │   │
│  │  │投资：1000万 │生产资产：   │产品特性：   │首单收入：   │     │   │
│  │  │成本：500万  │200万        │600元        │100万        │     │   │
│  │  └─────────────┴─────────────┴─────────────┴─────────────┘     │   │
│  │                                                                 │   │
│  │  ┌─────────────────────────────────────────────────────────┐   │   │
│  │  │  优化点识别                                              │   │   │
│  │  │  🔴 客户认知价值下降5%（首单收入下降）                   │   │   │
│  │  │  ⚠️  播传投资边际ROI偏低（15%）                          │   │   │
│  │  │  ✅ 交付投资边际ROI高（40%）                             │   │   │
│  │  └─────────────────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                           │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎨 网络节点设计

### 节点分类与样式

```typescript
// 节点类型定义
interface NetworkNode {
  id: string;
  label: string;
  type: 'investment' | 'asset' | 'capability' | 'process' | 'value' | 'revenue';
  category?: string; // 领域分类（生产、播传、首单、交付、追销、复购）
  value: number; // 当前值
  unit: string; // 单位
  changeRate: number; // 变化率
  trend: 'up' | 'down' | 'stable'; // 趋势
  status: 'good' | 'warning' | 'critical'; // 状态
  position: { x: number; y: number }; // 位置坐标
  connections: string[]; // 连接的其他节点ID
}

// 节点颜色编码
const NODE_COLORS = {
  // 输入层（黄色）
  investment: '#FFD700', // 投资 - 金黄色
  cost: '#FFA500', // 成本 - 橙色
  
  // 资产+能力层（绿色系）
  asset: {
    production: '#4CAF50', // 生产资产 - 绿色
    dissemination: '#66BB6A', // 播传资产 - 浅绿色
    firstOrder: '#81C784', // 首单资产 - 更浅绿色
    delivery: '#A5D6A7', // 交付资产 - 很浅绿色
    upsell: '#C8E6C9', // 追销资产 - 最浅绿色
    repurchase: '#E8F5E9' // 复购资产 - 极浅绿色
  },
  
  capability: {
    production: '#2E7D32', // 生产能力 - 深绿色
    dissemination: '#388E3C', // 播传能力 - 较深绿色
    firstOrder: '#43A047', // 首单能力 - 中绿色
    delivery: '#558B2F', // 交付能力 - 深绿色
    upsell: '#689F38', // 追销能力 - 中深绿色
    repurchase: '#7CB342' // 复购能力 - 中绿色
  },
  
  // 流程层（棕色系）
  process: {
    production: '#8B4513', // 生产流程 - 棕色
    dissemination: '#A0522D', // 播传流程 - 浅棕色
    firstOrder: '#CD853F', // 首单流程 - 更浅棕色
    delivery: '#D2691E', // 交付流程 - 浅棕色
    upsell: '#DEB887', // 追销流程 - 浅棕色
    repurchase: '#F4A460' // 复购流程 - 更浅棕色
  },
  
  // 价值要素层（黑色系）
  value: {
    feature: '#212121', // 产品特性 - 深黑色
    intrinsic: '#424242', // 产品内在价值 - 黑色
    perceived: '#616161', // 客户感知价值 - 灰色
    experienced: '#757575' // 客户体验价值 - 浅灰色
  },
  
  // 收益层（金色系）
  revenue: {
    firstOrder: '#FFD700', // 首单收入 - 金黄色
    upsell: '#FFA500', // 追销收入 - 橙色
    repurchase: '#FF8C00' // 复购收入 - 深橙色
  }
};

// 节点大小
const NODE_SIZES = {
  investment: { width: 120, height: 60 }, // 投资节点
  asset: { width: 100, height: 50 }, // 资产节点
  capability: { width: 100, height: 50 }, // 能力节点
  process: { width: 100, height: 50 }, // 流程节点
  value: { width: 120, height: 60 }, // 价值节点
  revenue: { width: 100, height: 50 } // 收益节点
};
```

---

## 🔗 连接线设计

### 连接线类型

```typescript
// 连接线定义
interface NetworkConnection {
  from: string; // 起始节点ID
  to: string; // 目标节点ID
  type: 'solid' | 'dashed' | 'dotted'; // 线型
  width: number; // 线宽（表示数据流量大小）
  color: string; // 颜色（表示转化效率）
  efficiency?: number; // 转化效率（0-1）
  arrow: 'forward' | 'bidirectional' | 'none'; // 箭头方向
  label?: string; // 连接标签（可选）
}

// 连接线颜色编码（根据转化效率）
const CONNECTION_COLORS = {
  high: '#4CAF50', // 高转化效率 - 绿色
  medium: '#FFC107', // 中转化效率 - 黄色
  low: '#F44336', // 低转化效率 - 红色
  default: '#9E9E9E' // 默认 - 灰色
};

// 连接线宽度（根据数据流量）
const CONNECTION_WIDTHS = {
  large: 4, // 大流量 - 4px
  medium: 2, // 中流量 - 2px
  small: 1 // 小流量 - 1px
};
```

### 连接关系映射

```typescript
// 连接关系定义
const CONNECTION_MAP = {
  // 投资 → 核心资产+能力
  'investment → production_asset': { type: 'solid', width: 'medium' },
  'investment → dissemination_asset': { type: 'solid', width: 'medium' },
  'investment → firstOrder_asset': { type: 'solid', width: 'medium' },
  'investment → delivery_asset': { type: 'solid', width: 'medium' },
  'investment → upsell_asset': { type: 'solid', width: 'medium' },
  'investment → repurchase_asset': { type: 'solid', width: 'medium' },
  
  // 核心资产+能力 → 流程
  'production_asset → production_process': { type: 'solid', width: 'large' },
  'production_capability → production_process': { type: 'solid', width: 'large' },
  'dissemination_asset → dissemination_process': { type: 'solid', width: 'large' },
  'dissemination_capability → dissemination_process': { type: 'solid', width: 'large' },
  // ... 其他流程连接
  
  // 流程 → 价值要素
  'production_process → feature_value': { type: 'solid', width: 'medium' },
  'feature_value → intrinsic_value': { type: 'solid', width: 'large' },
  'intrinsic_value → dissemination_process': { type: 'solid', width: 'medium' },
  'intrinsic_value → delivery_process': { type: 'solid', width: 'medium' },
  'dissemination_process → perceived_value': { type: 'solid', width: 'large' },
  'delivery_process → experienced_value': { type: 'solid', width: 'large' },
  'perceived_value → experienced_value': { type: 'dashed', width: 'small' }, // 反馈关系
  
  // 价值要素 → 流程 → 收益
  'perceived_value → firstOrder_process': { type: 'solid', width: 'large' },
  'perceived_value → repurchase_process': { type: 'solid', width: 'medium' },
  'experienced_value → upsell_process': { type: 'solid', width: 'large' },
  'experienced_value → repurchase_process': { type: 'solid', width: 'large' },
  'firstOrder_process → firstOrder_revenue': { type: 'solid', width: 'large' },
  'upsell_process → upsell_revenue': { type: 'solid', width: 'large' },
  'repurchase_process → repurchase_revenue': { type: 'solid', width: 'large' }
};
```

---

## 📊 网络布局算法

### 力导向布局（Force-Directed Layout）

```typescript
// 力导向布局配置
interface ForceLayoutConfig {
  // 节点间距
  nodeDistance: {
    sameLevel: number; // 同层级节点间距
    differentLevel: number; // 不同层级节点间距
  };
  
  // 力参数
  forces: {
    center: number; // 中心力
    collision: number; // 碰撞力
    link: number; // 连接力
    charge: number; // 电荷力（排斥力）
  };
  
  // 层级排列
  levels: {
    level1: 'investment'; // 第1层：投资
    level2: 'asset_capability'; // 第2层：资产+能力
    level3: 'process'; // 第3层：流程
    level4: 'value'; // 第4层：价值要素
    level5: 'revenue'; // 第5层：收益
  };
}

// 布局算法实现
class ForceDirectedLayout {
  private nodes: NetworkNode[];
  private connections: NetworkConnection[];
  private config: ForceLayoutConfig;
  
  calculateLayout(): NetworkNode[] {
    // 1. 初始化节点位置（按层级排列）
    this.initializePositions();
    
    // 2. 应用力导向算法
    for (let iteration = 0; iteration < 100; iteration++) {
      this.applyForces();
      this.updatePositions();
    }
    
    // 3. 优化连接线
    this.optimizeConnections();
    
    return this.nodes;
  }
  
  private initializePositions() {
    // 按层级排列节点（自下而上，体现支撑关系）
    const levelPositions = {
      // 第1层（底部）：投资与成本 - 基础支撑层
      investment: { y: 50, xRange: [400, 600] },
      
      // 第2层：核心资产+能力 - 支撑层
      asset_capability: { y: 150, xRange: [100, 900] },
      
      // 第3层：流程层 - 转化层
      process: { y: 250, xRange: [100, 900] },
      
      // 第4层：价值要素层 - 网络交汇点
      value: { y: 350, xRange: [300, 700] },
      
      // 第5层（顶部）：收益层 - 目标层
      revenue: { y: 450, xRange: [300, 700] }
    };
    
    // 箭头方向：从下往上（支撑关系）
    // 第1层（投资）→ 第2层（资产+能力）→ 第3层（流程）→ 第4层（价值）→ 第5层（收益）
    
    // ... 布局逻辑
  }
}
```

### 层级布局（自下而上展示支撑关系）

```typescript
// 自下而上层级布局配置（体现支撑关系）
interface HierarchicalLayoutConfig {
  levels: {
    level1: { nodes: string[], y: number }; // 第1层（底部）：投资与成本
    level2: { nodes: string[], y: number }; // 第2层：资产+能力
    level3: { nodes: string[], y: number }; // 第3层：流程层
    level4: { nodes: string[], y: number }; // 第4层：价值要素层
    level5: { nodes: string[], y: number }; // 第5层（顶部）：收益层
  };
  
  horizontalSpacing: number; // 水平间距
  verticalSpacing: number; // 垂直间距
  
  // 支撑关系标识
  supportDirection: 'bottom-to-top'; // 从下往上支撑
}

// 自下而上层级布局算法
class HierarchicalLayout {
  private nodes: NetworkNode[];
  private config: HierarchicalLayoutConfig;
  
  calculateLayout(): NetworkNode[] {
    // 1. 按层级分组节点（从下往上）
    const nodeGroups = this.groupNodesByLevel();
    
    // 2. 计算每层的水平位置（自下而上排列）
    // 第1层（底部）：投资与成本 - 基础支撑
    // 第5层（顶部）：收益层 - 目标
    nodeGroups.forEach((level, levelIndex) => {
      const spacing = this.config.horizontalSpacing;
      const startX = (width - (level.length - 1) * spacing) / 2;
      
      // 从下往上排列（y值递增）
      const baseY = 50; // 底部起始位置
      const yOffset = levelIndex * this.config.verticalSpacing;
      
      level.forEach((node, nodeIndex) => {
        node.position.x = startX + nodeIndex * spacing;
        node.position.y = baseY + yOffset; // 从下往上递增
      });
    });
    
    return this.nodes;
  }
}
```

---

## 🎯 交互功能设计

### 节点交互

```typescript
// 节点交互功能
interface NodeInteraction {
  // 点击节点：显示详细信息
  onNodeClick: (nodeId: string) => void;
  
  // 悬停节点：显示快速信息
  onNodeHover: (nodeId: string) => void;
  
  // 拖拽节点：调整位置
  onNodeDrag: (nodeId: string, newPosition: { x: number; y: number }) => void;
  
  // 多选节点：批量操作
  onNodeSelect: (nodeIds: string[]) => void;
}

// 节点详细信息面板
interface NodeDetailPanel {
  nodeId: string;
  title: string;
  metrics: {
    currentValue: number;
    unit: string;
    changeRate: number;
    trend: 'up' | 'down' | 'stable';
    status: 'good' | 'warning' | 'critical';
  };
  connections: {
    incoming: string[]; // 输入的连接
    outgoing: string[]; // 输出的连接
  };
  analysis: {
    marginalImpact: number; // 边际影响
    efficiency: number; // 效率
    recommendations: string[]; // 优化建议
  };
}
```

### 连接线交互

```typescript
// 连接线交互功能
interface ConnectionInteraction {
  // 点击连接线：显示转化效率详情
  onConnectionClick: (connectionId: string) => void;
  
  // 悬停连接线：显示数据流量
  onConnectionHover: (connectionId: string) => void;
  
  // 路径高亮：高亮从某个节点到另一个节点的路径
  onPathHighlight: (fromNodeId: string, toNodeId: string) => void;
}

// 连接线详情面板
interface ConnectionDetailPanel {
  connectionId: string;
  from: string;
  to: string;
  metrics: {
    efficiency: number; // 转化效率
    dataFlow: number; // 数据流量
    changeRate: number; // 变化率
  };
  analysis: {
    bottleneck: boolean; // 是否为瓶颈
    recommendation: string; // 优化建议
  };
}
```

---

## 📈 可视化增强功能

### 1. 路径追踪

```typescript
// 路径追踪功能
interface PathTracing {
  // 追踪从投资到收益的完整路径
  tracePath(investmentNodeId: string, revenueNodeId: string): NetworkNode[];
  
  // 高亮路径
  highlightPath(path: NetworkNode[]): void;
  
  // 显示路径上的所有指标
  showPathMetrics(path: NetworkNode[]): PathMetrics;
}

// 示例：追踪播传投资到首单收入的路径
const path = tracePath('dissemination_investment', 'firstOrder_revenue');
// 路径：投资 → 播传资产+能力 → 播传流程 → 客户感知价值 → 首单流程 → 首单收入
```

### 2. 影响力分析

```typescript
// 影响力分析
interface InfluenceAnalysis {
  // 计算某个节点的影响范围
  calculateInfluence(nodeId: string): {
    directInfluence: string[]; // 直接影响
    indirectInfluence: string[]; // 间接影响
    totalImpact: number; // 总影响值
  };
  
  // 可视化影响范围
  visualizeInfluence(nodeId: string): void;
}
```

### 3. 瓶颈识别可视化

```typescript
// 瓶颈识别可视化
interface BottleneckVisualization {
  // 识别瓶颈节点
  identifyBottlenecks(): NetworkNode[];
  
  // 高亮瓶颈节点
  highlightBottlenecks(bottlenecks: NetworkNode[]): void;
  
  // 显示瓶颈分析
  showBottleneckAnalysis(bottleneckId: string): BottleneckAnalysis;
}
```

---

## 💻 技术实现

### 推荐技术栈

```typescript
// 网络图可视化库
- D3.js（推荐）：强大的网络图可视化库
- vis.js：轻量级网络图库
- Cytoscape.js：专业的网络图库

// React集成
- react-d3-graph：React + D3.js 集成
- react-vis-graph：React + vis.js 集成
- react-cytoscapejs：React + Cytoscape.js 集成

// 布局算法
- D3.js Force Layout：力导向布局
- D3.js Hierarchical Layout：层级布局
- Dagre：有向无环图布局
```

### 核心组件实现

```typescript
// 网络图主组件
interface ValueChainNetworkGraphProps {
  data: {
    nodes: NetworkNode[];
    connections: NetworkConnection[];
  };
  layout: 'force' | 'hierarchical' | 'custom';
  interactions: NodeInteraction & ConnectionInteraction;
  onNodeClick?: (nodeId: string) => void;
  onConnectionClick?: (connectionId: string) => void;
}

// React组件示例
const ValueChainNetworkGraph: React.FC<ValueChainNetworkGraphProps> = ({
  data,
  layout,
  interactions,
  onNodeClick,
  onConnectionClick
}) => {
  // 使用D3.js或vis.js渲染网络图
  // ...
  
  return (
    <div className="value-chain-network-graph">
      {/* 网络图SVG/Canvas */}
      {/* 节点 */}
      {/* 连接线 */}
      {/* 交互元素 */}
    </div>
  );
};
```

---

## 📋 数据模型

### 完整数据模型

```typescript
// 价值链网络数据模型
interface ValueChainNetworkData {
  // 节点数据
  nodes: {
    // 输入层
    investments: NetworkNode[]; // 投资节点
    costs: NetworkNode[]; // 成本节点
    
    // 资产+能力层
    assets: NetworkNode[]; // 资产节点（6个领域）
    capabilities: NetworkNode[]; // 能力节点（6个领域）
    
    // 流程层
    processes: NetworkNode[]; // 流程节点（6个流程）
    
    // 价值要素层
    values: NetworkNode[]; // 价值节点（4个价值要素）
    
    // 收益层
    revenues: NetworkNode[]; // 收益节点（3种销售额）
  };
  
  // 连接数据
  connections: NetworkConnection[];
  
  // 元数据
  metadata: {
    tenantId: string;
    monthDate: Date;
    calculationDate: Date;
    version: string;
  };
}
```

---

## 🎨 视觉效果设计

### 状态颜色编码

```typescript
// 节点状态颜色
const STATUS_COLORS = {
  good: {
    fill: '#E8F5E9', // 浅绿色背景
    border: '#4CAF50', // 绿色边框
    text: '#2E7D32' // 深绿色文字
  },
  warning: {
    fill: '#FFF9C4', // 浅黄色背景
    border: '#FFC107', // 黄色边框
    text: '#F57C00' // 橙色文字
  },
  critical: {
    fill: '#FFEBEE', // 浅红色背景
    border: '#F44336', // 红色边框
    text: '#C62828' // 深红色文字
  }
};

// 连接线状态颜色
const CONNECTION_STATUS_COLORS = {
  high: '#4CAF50', // 高转化效率 - 绿色
  medium: '#FFC107', // 中转化效率 - 黄色
  low: '#F44336', // 低转化效率 - 红色
  default: '#9E9E9E' // 默认 - 灰色
};
```

### 动画效果

```typescript
// 动画配置
const ANIMATIONS = {
  nodeHover: {
    duration: 200, // 200ms
    scale: 1.1 // 放大1.1倍
  },
  connectionHover: {
    duration: 200,
    width: 6 // 线宽变为6px
  },
  pathHighlight: {
    duration: 500,
    color: '#2196F3' // 高亮颜色 - 蓝色
  }
};
```

---

## 📚 相关文档

- [详细价值链设计](./VALUE_CHAIN_DETAILED.md) - 价值链架构详细说明
- [价值链优化方法论](./VALUE_CHAIN_OPTIMIZATION_METHODOLOGY.md) - 优化方法论
- [管理仪表盘设计](./EXECUTIVE_DASHBOARD_DESIGN.md) - 管理仪表盘设计

---

**文档版本**: 2.0  
**最后更新**: 2025-01-23  
**状态**: ✅ **多流程网络结构可视化设计方案完整说明**

